import React from 'react';
import Header from '../components/Header/Header';

const Error = () => {
  return (
    <div>
      <Header/>
      Page can't be found.
    </div>
  );
};

export default Error;